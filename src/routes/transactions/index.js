import { h } from 'preact';
import style from './style';

const Transactions = () => (
	<div class={style.transactions}>
		<h1>Transactions</h1>
		<p>This is the transactions component.</p>
	</div>
);

export default Transactions;
